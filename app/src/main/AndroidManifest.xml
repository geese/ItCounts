<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sixgeese.itcounts">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CalendarActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DayDetailActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SetLabelsActivity"
            android:parentActivityName=".DayDetailActivity" />
        <activity
            android:name=".RepLabelsActivity"
            android:parentActivityName=".DayDetailActivity" />
        <activity
            android:name=".EditLabelsActivity"
            android:parentActivityName=".DayDetailActivity" />
    </application>

</manifest>